# ── Credentials & secrets (never commit these) ──────────────────────────
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.secret
*credentials*
*token*
*password*
*api_key*
*apikey*
secrets/
creds/
auth.json
config.json
*.dat

# ── Trading / MT5 artifacts (local only) ────────────────────────────────
# Active bot sources + binaries ARE tracked (2026-09-17, user directive —
# new 24/7 server clones the repo). Old/retired bots stay local.
*.log
*.csv
!raw/trades/*.csv
Account_Info.csv
Account_History.csv
*.tpl
*.set

# ── OS / editor noise ───────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store
*.swp
.idea/
.vscode/